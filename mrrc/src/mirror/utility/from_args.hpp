#pragma once

namespace mirror {
	/**
	 * @brief Run mirror compiler with arguments (this is a utility function for standalone compiler)
	 * @param argc Number of arguments
	 * @param argv Arguments
	*/
	void from_args(int argc, char** argv);
}
